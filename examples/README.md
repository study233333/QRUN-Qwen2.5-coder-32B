Examples
--------

1. `simple_inference.py` - Basic single-GPU inference example. Usage:

```bash
python examples/simple_inference.py --model-path PATH_TO_WEIGHTS_DIR --prompt "print(1+1)"
```

Note: This example focuses on demonstrating API calls. Loading the full weights requires adequate VRAM and multi-GPU support in reality.
